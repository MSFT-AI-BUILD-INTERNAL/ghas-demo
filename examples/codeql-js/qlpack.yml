name: examples-codeql-js
version: 0.0.1
dependencies:
  codeql/javascript-all: "*"
# 이 팩은 "CodeQL 문법 학습·시연용 예제"입니다.
# 실제 스캔(codeql.yml, languages: python)에는 포함되지 않습니다 —
# custom-queries/ 폴더 밖에 있고 config-file 이 참조하지 않기 때문.
